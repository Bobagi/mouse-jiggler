{
  "name": "mouse-jiggler",
  "version": "1.0.0",
  "description": "Mouse Jiggler in javascript that moves the mouse cursor periodically to prevent the operating system from entering sleep mode or screen lock",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "robotjs": "^0.6.0"
  }
}
